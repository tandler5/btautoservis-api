extend type Query {
    timeBlockInterval: Int
    socialLogin: JSON
    taxes: [Tax!]!
    customFields: JSON
}

type TaxEntry {
    value: Tax
}

type Tax {
    id: ID
    name: String
    type: String
    value: Int
}
