type Query {
    timeBlockInterval: Int
    taxes: [Tax!]!
    customFields: JSON
}

type TaxEntry {
    value: Tax
}

type Tax {
    id: ID
    name: String
    name: String
    type: String
    value: Int
}
